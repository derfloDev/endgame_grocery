# TASKS

Use this board to coordinate handoff between planner, implementer, and reviewer.

Status values:
- `in_planning`
- `ready_for_implement`
- `in_implementation`
- `ready_for_review`
- `in_review`
- `ready_to_commit`
- `changes_requested`
- `done`

Command expectations:
- planner moves tasks into `in_planning` and `ready_for_implement`
- implementer moves tasks into `in_implementation`, `ready_for_review`, and `done`, and resumes work from `changes_requested` and `ready_to_commit`
- reviewer moves tasks into `in_review`, `ready_to_commit`, or `changes_requested`
- `status_cycle` should report deterministic task status, current owner role, and next recommended action based on this board

| Task ID | Scope | Status | Acceptance Criteria | Evidence | Next Role |
| --- | --- | --- | --- | --- | --- |
| T-001 | Load the icon model on first use instead of at startup (`main.tsx`, `iconWorkerClient.ts`, `AddItemSheet.tsx`) | done | Cold load of `/lists/<id>` requests no `huggingface.co`, `hf.co` or `cdn.jsdelivr.net` host; adding an entry still yields an icon suggestion with a visible loading state; the model is fetched on sheet open only, with no idle or speculative warm-up and no `warmIconWorkerWhenIdle` occurrence left in the repository | 2026-09-20T17:34:32Z rework: unused idle helper/tests/docs removed; lint/build and 756 tests pass; cold-load Chromium spec passes on retry after a visibility timeout. Transfer budget belongs to T-006 per revised plan. Identifier references remain only in .ai workflow records; see HANDOFF.md. 2026-09-20T18:04:53Z review round 2 `PASS_WITH_NOTES`: lint/build PASS, backend 174/174, frontend 582/582 on re-run, cold-load E2E spec PASS; production-build browser check shows 0 workers and 0 model requests on cold load, then 1 worker and a `huggingface.co` fetch on sheet open, spinner for a non-catalogue term, instant exact match. Before committing, keep untracked `get-docker.sh` and `.claude/settings.local.json` out of the `git add -A`. | none |
| T-002 | Load the detail page exactly once and render entries without waiting for members (`useListDetailData.ts`, `listDetailUtils.ts`, `ListDetailPage.tsx`) | done | Each of `/api/lists`, `/entries`, `/history`, `/members`, `/mark-viewed` requested exactly once per visit even after lazy i18n resources arrive; entries visible as soon as `/entries` resolves; access error and member-load failures still surface translated | 2026-09-20T18:25:25Z: test-first regressions verified; lint/build PASS; 762 unit tests PASS; 3 production Chromium scenarios PASS (delayed translations/members, member failure, translated access denial), owner endpoints exactly once. Extra tsc check reports 3 pre-existing errors; see HANDOFF.md. 2026-09-20T18:41:51Z review round 1 `PASS_WITH_NOTES`: lint/build PASS, 588 frontend + 174 backend tests PASS; the 3 tsc errors confirmed pre-existing by re-running the typecheck against the stashed baseline; production-build browser scenarios show all five endpoints requested exactly once with translations held 2.5 s and members held 4 s, entries visible before members, member 503 keeps list and entries with a banner, and a missing list renders the German access error with no members/mark-viewed call. Keep `get-docker.sh` and `.claude/settings.local.json` out of the `git add -A`. | none |
| T-003 | Render cached list data immediately, replace it with the network response (`client.ts`, `entries.ts`, `lists.ts`, `useListDetailData.ts`) | done | With a populated cache, entries render as soon as the asynchronous cache read completes, without waiting for the network or showing a spinner over cached content, then get replaced by the server payload; cached value never applied after the network response; existing offline fallback and pending-entry merge unchanged | 2026-09-21T04:59:12Z: user accepted rendering as soon as the cache read completes; initial loading frame before cache completion is acceptable. Existing validation: lint/build and 779 tests PASS; 4 production Chromium cache scenarios PASS. Timing wording aligned in PLAN.md and ROADMAP.md; see HANDOFF.md. 2026-09-21T05:24:58Z review round 1 `PASS_WITH_NOTES`: lint/build PASS, 605 frontend + 174 backend tests PASS, no new tsc errors; production-build browser run against real IndexedDB shows cached entries and title at 257 ms with no spinner while the network is held 6 s, replaced by the server payload at 6227 ms, offline fallback intact, cache miss still shows the loading state, and revoked access clears the cached view with the translated error. Minor follow-up for the planner: cache keys are not scoped per account and logout does not clear them. | none |
| T-004 | Stop probing `/api/health` at startup when the offline queue is empty (`OfflineQueueContext.tsx`) | done | No `/api/health` request on mount with an empty queue; a queued mutation still probes, drains, and retries; offline banner and recovery behaviour unchanged | 2026-09-21T16:18:54Z: own request deadlines now refresh reachability while parent cancellations remain ignored; timeout-to-banner regression added; lint/build and full 609 frontend + 174 backend tests pass; review round 2 finding addressed 2026-09-22T04:24:09Z review round 3 `PASS`: lint/build PASS, 609 frontend tests PASS; all four production-build scenarios now match the pre-T-004 baseline — healthy load 0 probes and no banner, refused 1 probe with banner, hung backend 1 probe with banner, known-offline recovery clears the banner on `online` with an empty queue; queued-mutation drain re-verified, parent cancellations still suppress the probe, probe volume bounded at 1 per 25 s, and T-003 cache-first re-checked unaffected. Commit 2026-09-22T04:27:33Z: committed after PASS review as `perf(sync): stop probing the server at startup when nothing is queued`. | none |
| T-005 | Self-host and precache the app fonts (`index.html`, `frontend/public/fonts/`, `vite.config.ts`) | done | No request to `fonts.googleapis.com` or `fonts.gstatic.com`; Lighthouse reports no third-party render-blocking stylesheet; typography visually unchanged; fonts render while offline; licences documented | 2026-09-22T08:51:43Z: removed third-party Google Fonts links; self-hosted three current families as five Latin/Latin-Extended WOFF2 subsets with OFL texts and attribution; precache includes all five; lint/build and 612 frontend + 174 backend tests pass; Chromium verifies local and offline font loading 2026-09-22T10:24:40Z review round 1 `PASS_WITH_NOTES`: lint/build PASS, 612 frontend tests PASS; self-hosted build requests only `localhost` against 3 Google-host requests from the stashed baseline; full-page screenshots pixel-identical to baseline (0/1,152,000 differing pixels, repeated with Latin-Extended text); fonts served from Cache Storage with the origin killed, all five subsets precached; OFL texts and attribution present. Minor for T-006: precache grew 124.84 KiB while removing only the 52.1 KiB Google pair, a net +73 KiB on cold load — PLAN.md Phase 6's table line is now wrong in sign and the budget must be re-measured. | implement |
| T-006 | Cut the cold-load transfer weight of the PWA icons, the logo and the precache manifest (`frontend/public/icon-512.png`, `frontend/public/icon-192.png`, `frontend/src/assets/endgame_grocery_logo.png`, `vite.config.ts`, `vite-config.test.ts`) | ready_for_implement | Cold load of `/lists/<id>` transfers < 500 KiB with no single response over 150 KiB, measured after T-005 has landed; `assets/iconWorker-*.js` is no longer precached; PWA icons stay 192×192 and 512×512 with `purpose: "any maskable"` and the app still installs and opens offline; icons and logo visually unchanged at rendered sizes | n/a | implement |
